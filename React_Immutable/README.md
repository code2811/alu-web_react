# React Immutable

Immutable.js exercises covering immutable maps, lists, nested access and merging, equality, mutations, and lazy sequences.

## Setup

```bash
npm install
```

## Tests

```bash
npm test
```

The exercise implementations are in `0-fromjs.js` through `8-seq.js`.